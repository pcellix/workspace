document.observe("dom:loaded", function() {
	$('ja').setStyle({
		backgroundColor: '#900',
		fontSize: '12px'
	});
	$$('a').each(
			   function (e) {
			      e.setStyle({color:'green'}); 
			   } 
			);
	
});
