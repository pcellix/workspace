document.observe("dom:loaded", function() {
	$('ja').setStyle({
		backgroundColor: '#900',
		fontSize: '12px'
	});
	$$('.black').each(
			   function (e) {
			      e.setStyle({border:'1px solid red'}); 
			   } 
			);
});
